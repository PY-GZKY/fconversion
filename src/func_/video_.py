# from moviepy.editor import VideoFileClip
def video2audio(source_file: str, target_file: str):
    """
    video = VideoFileClip(source_file)
    audio = video.audio
    audio.write_audiofile(target_file)
    """
